The password for the Administrator account saved in the database is 'admin1'.
If you want to change it to something else just use Bcrypt to hash a new password so PHP will not fail at comparing the hashes.